# Office-tool

Didar household utility: React UI in `web/`, CLI in `didar_household_tool.py`.

- Web: `./run.sh` (or `cd web && npm run dev`)
- CLI: `./run-cli.sh` or `python didar_household_tool.py --cli`
